# apps/marketing/src/styles

`global.css` is the Tailwind entry. It also defines a `prose-grassion` utility for long-form prose pages (about, privacy, terms) so we don't depend on `@tailwindcss/typography`.

Add new utilities sparingly — Tailwind classes inline on the page are fine for one-offs.
