[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --prefix server"]

[phases.build]
cmds = ["npm run build --prefix server"]

[start]
cmd = "node server/dist/index.js"

[variables]
NODE_ENV = "production"
